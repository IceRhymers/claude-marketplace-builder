## 0. Test Infrastructure Additions

- [ ] 0.1 (RED) Add `mock_workspace_files` to `mock_workspace_client` fixture in `tests/conftest.py`: add `client.files` as a `MagicMock` with `upload`, `download`, `list`, and `delete` sub-mocks; `list` returns an empty list by default; `upload`/`download`/`delete` are no-ops by default
- [ ] 0.2 (RED) Add `session_dir_with_files` fixture to `tests/conftest.py`: creates a `tmp_path`-backed directory with two pre-written test files (`output.csv` with CSV content, `results.txt` with plain text)
- [ ] 0.3 (RED) Add `populated_messages_db` fixture to `tests/conftest.py`: a `db_session` variant that pre-inserts one `Conversation` row and 4 `Message` rows (alternating `role=user`/`role=assistant`) for a fixed `TEST_CONVERSATION_ID = "test-conv-001"`, with `created_at` timestamps 1 second apart to allow ordering assertions
- [ ] 0.4 (RED) Add `mock_volume_with_files` fixture to `tests/conftest.py`: a mock of `WorkspaceClient.files.list()` that returns a list of 2 `FileInfo`-like mock objects (with `path` attributes), and `download()` that returns stub `bytes` content per file path
- [ ] 0.5 (RED) Add `mock_volume_empty` fixture to `tests/conftest.py`: a mock of `WorkspaceClient.files.list()` that returns an empty list (path exists but no files)
- [ ] 0.6 (RED) Add `stale_conversations_db` fixture to `tests/conftest.py`: a `db_session` variant with 2 `Conversation` rows having `updated_at` set to 35 days ago and 1 `Conversation` row with `updated_at` set to 5 days ago
- [ ] 0.7 (RED) Add `mock_scheduler` fixture to `tests/conftest.py`: a `MagicMock` of `BackgroundScheduler` with `add_job` and `start` as `MagicMock`s, capturing all `add_job` calls for assertion
- [ ] 0.8 (GREEN) Verify all new fixtures are importable by running `pytest --collect-only tests/` with zero errors; no test files written yet beyond fixture additions

## 1. History Hydration

- [ ] 1.1 (RED) Write `tests/unit/test_history_hydration.py`: write failing tests for all `history-hydration` spec scenarios — cache miss with no messages yields empty `_history`; cache miss with 3 messages yields 3 `_history` entries in correct order; cache hit does not query DB; messages mapped to `{"role": ..., "content": ...}`; DB failure during hydration propagates as `RuntimeError` and agent not stored in pool
- [ ] 1.2 (GREEN) Extend `AgentPool.get_or_create` signature in `core/agent_pool.py` to accept a `db: Session` parameter; on cache miss, query `Message` rows for `conversation_id` ordered by `created_at` ascending and inject into `agent._history` as `[{"role": m.role, "content": m.content}]` before returning; wrap in try/except — on failure, do not store the entry and re-raise as `RuntimeError`
- [ ] 1.3 (GREEN) Update all call sites of `AgentPool.get_or_create` (in `routers/stream.py`) to pass the `db` session dependency; run `pytest tests/unit/test_history_hydration.py` — all tests must pass

## 2. File Sync on Eviction

- [ ] 2.1 (RED) Write `tests/unit/test_file_sync_eviction.py`: write failing tests for all `file-sync-on-eviction` spec scenarios — non-empty session dir synced to correct Volume path; empty session dir skips upload; upload failure logs WARNING and does not block eviction; `purge=True` skips upload; `AGENT_SESSIONS_VOLUME_PATH` unset skips upload with WARNING; `evict_stale()` calls `evict()` with `purge=False` (default)
- [ ] 2.2 (GREEN) Add `purge: bool = False` parameter to `AgentPool.evict()` in `core/agent_pool.py`; before deleting the local `session_dir`, check `AGENT_SESSIONS_VOLUME_PATH` env var and `purge` flag; if Volume path set and `purge=False` and session dir non-empty, iterate files and call `WorkspaceClient.files.upload()` for each file to `{AGENT_SESSIONS_VOLUME_PATH}/{entry.user_id}/{conversation_id}/{relative_path}`; wrap upload in try/except that logs WARNING on failure; after upload attempt (success or warning), delete local session dir; run `pytest tests/unit/test_file_sync_eviction.py` — all tests must pass
- [ ] 2.3 (GREEN) Inject `WorkspaceClient` into `AgentPool` (constructor or via `core/auth.py` helper): add `_workspace_client` attribute initialized from `WorkspaceClient()` at pool creation time; this client is used for all Volume operations; run `pytest tests/unit/test_agent_pool.py tests/unit/test_file_sync_eviction.py` — all tests must pass

## 3. File Restore on Resume

- [ ] 3.1 (RED) Write `tests/unit/test_file_restore_resume.py`: write failing tests for all `file-restore-on-resume` spec scenarios — cache miss with Volume files downloads all files to local sandbox; cache miss with no Volume path makes no download calls; download failure logs WARNING and does not raise from `get_or_create`; restore occurs before history hydration (assert call order via mock); cache hit makes no download calls; `AGENT_SESSIONS_VOLUME_PATH` unset skips restore with WARNING
- [ ] 3.2 (GREEN) Extend `AgentPool.get_or_create` cache-miss path in `core/agent_pool.py`: FIRST check Volume path `{AGENT_SESSIONS_VOLUME_PATH}/{user_id}/{conversation_id}/` by calling `WorkspaceClient.files.list()`; if files returned, download each to the local session sandbox via `WorkspaceClient.files.download()`; wrap in try/except that logs WARNING on failure and continues; THEN proceed to agent spawn and history hydration (restore before hydration ordering per D5); run `pytest tests/unit/test_file_restore_resume.py` — all tests must pass

## 4. Conversation Delete Cleanup

- [ ] 4.1 (RED) Write `tests/unit/test_conversation_delete_cleanup.py`: write failing tests for all `conversation-delete-cleanup` spec scenarios — DELETE owned conversation calls `evict(purge=True)`, calls `files.delete` with correct Volume path, returns `204`, DB row gone; `files.delete` failure logs WARNING and `204` still returned; DELETE non-owned returns `404` with no side effects; conversation not in pool still triggers Volume delete; `AGENT_SESSIONS_VOLUME_PATH` unset skips `files.delete` and returns `204`
- [ ] 4.2 (GREEN) Extend `DELETE /api/conversations/{id}` in `routers/conversations.py`: after ownership check, call `pool.evict(conversation_id, purge=True)`; then attempt `WorkspaceClient.files.delete(volume_path, recursive=True)` wrapped in try/except that logs WARNING on failure; then delete the DB row; return `204`; resolve `user_id` from `current_user` to construct Volume path; check `AGENT_SESSIONS_VOLUME_PATH` before attempting Volume delete; run `pytest tests/unit/test_conversation_delete_cleanup.py` — all tests must pass

## 5. Conversation TTL Cleanup

- [ ] 5.1 (RED) Write `tests/unit/test_ttl_cleanup.py`: write failing tests for all `conversation-ttl-cleanup` spec scenarios — 2 stale + 1 active: only stale purged; 0 stale: no-ops; `CONVERSATION_TTL_DAYS=7` uses 7-day threshold; Volume delete failure for one conversation is isolated (other conversation still purged); `AGENT_SESSIONS_VOLUME_PATH` unset skips Volume deletes but deletes DB rows; job registered in scheduler with correct interval hours; job runs at startup (assert immediate run)
- [ ] 5.2 (GREEN) Create `claude-agent-app/app/core/cleanup.py` with `purge_stale_conversations(session_factory, pool, workspace_client, ttl_days, volume_path)` function: query `conversations` for rows where `updated_at < now() - ttl_days days`; for each: call `pool.evict(conversation_id, purge=True)`, attempt `workspace_client.files.delete(volume_path)` wrapped in try/except WARNING, delete DB row in a try/except that logs ERROR but continues; log total purged count; run `pytest tests/unit/test_ttl_cleanup.py` — all tests must pass
- [ ] 5.3 (GREEN) Register TTL cleanup job in `main.py` lifespan startup: add `CONVERSATION_TTL_DAYS` (default 30) and `CONVERSATION_TTL_CHECK_HOURS` (default 24) to `AppConfig`; add to `app.yml` env var list; register `purge_stale_conversations` with APScheduler using `"interval"` trigger with `hours=config.conversation_ttl_check_hours`; call once immediately at startup via `scheduler.add_job(..., next_run_time=datetime.now())`; run `pytest tests/unit/test_ttl_cleanup.py` — all tests must pass
- [ ] 5.4 (GREEN) Add `CONVERSATION_TTL_DAYS` and `CONVERSATION_TTL_CHECK_HOURS` env vars to `claude-agent-app/app.yml` and `claude-agent-app/resources/app.yml` with default values; add both to `AppConfig.from_env()` in `core/config.py`; run `pytest tests/` — full test suite must be green

## 6. Integration and Verification

- [ ] 6.1 (RED) Write `tests/integration/test_session_persistence_e2e.py`: write failing integration tests — full round-trip: agent writes file to session dir → eviction syncs to Volume → fresh `get_or_create` restores file → file exists in new session dir; history hydration + file restore both happen on single cache miss; DELETE conversation: pool evicted, Volume path deleted, DB row gone; TTL job purges only stale conversations; all using `mock_workspace_client` (no real Volume calls)
- [ ] 6.2 (GREEN) Run integration tests: `pytest tests/integration/test_session_persistence_e2e.py` — all must pass with mocked Volume client
- [ ] 6.3 (GREEN) Run full test suite: `pytest tests/` — all tests (pre-existing + new) must be green with no regressions introduced by the session-persistence changes
