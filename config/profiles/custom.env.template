# ==============================================================================
# Custom Endpoint — Inference Profile
#
# For any Anthropic-compatible proxy or gateway endpoint.
# Set the base URL and auth token for your custom backend.
#
# Docs: https://code.claude.com/docs/en/llm-gateway
# ==============================================================================

# Full URL to your custom inference endpoint
ANTHROPIC_BASE_URL={{CUSTOM_BASE_URL}}

# Auth token — leave blank to use shell environment
ANTHROPIC_AUTH_TOKEN={{AUTH_TOKEN}}
